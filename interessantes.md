# Wissenswertes und Anmerkungen aus der Vorlesung

## Warum geht `++` nicht in Python?
- bei einem `+` wird intern `__add__` aufgerufen, also bei `++` zwei mal `__add__` hintereinander ohne Datenbezug